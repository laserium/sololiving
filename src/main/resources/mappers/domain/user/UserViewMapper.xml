<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sololiving.domain.user.mapper.UserViewMapper">

    <select id="selectUserList" resultType="com.sololiving.domain.user.dto.response.ViewUserListResponseDto">
        SELECT
            user_id AS userId,
            email,
            contact,
            status,
            user_type AS userType,
            created_at AS createdAt,
            updated_at AS updatedAt,
            last_sign_in_at AS lastSignInAt,
            last_activity_at AS lastActivityAt
        FROM USER
    </select>

    <select id="isUserDeleted" resultType="boolean" parameterType="string">
        SELECT CASE WHEN status = 'WITHDRAW' THEN TRUE ELSE FALSE END
        FROM USER
        WHERE user_id = #{userId}
    </select>


</mapper>
