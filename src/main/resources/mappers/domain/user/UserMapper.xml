<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sololiving.domain.user.mapper.UserMapper">

    <!--회원가입-->
    <insert id="insertUser" parameterType="com.sololiving.domain.vo.UserVo">
        INSERT INTO USER (user_id, user_pwd, oauth2_user_id, nickname, contact, email, gender, address, birth, status, followers_cnt, following_cnt, created_at, updated_at, user_type, last_sign_in_at, last_activity_at)
        VALUES (#{userId}, #{userPwd}, #{oauth2UserId}, #{nickname}, #{contact}, #{email}, #{gender}, #{address}, #{birth}, #{status}, #{followersCnt}, #{followingCnt}, #{createdAt}, #{updatedAt}, #{userType}, #{lastSignInAt}, #{lastActivityAt})
    </insert>


    <!-- 회원탈퇴 -->
    <delete id="deleteByUserId" parameterType="string">
        DELETE
        FROM USER
        WHERE user_id = #{userId}
    </delete>

    <update id="updateUserStatus" parameterType="map">
        UPDATE USER
        SET status = #{status}
        WHERE user_id = #{userId}
    </update>

</mapper>
