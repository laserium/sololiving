<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sololiving.domain.auth.mapper.RefreshTokenMapper">

    <select id="findByUserId" parameterType="String" resultType="com.sololiving.domain.vo.RefreshTokenVo">
        SELECT * FROM REFRESH_TOKEN WHERE user_id = #{userId}
    </select>

    <insert id="save" parameterType="com.sololiving.domain.vo.RefreshTokenVo">
        INSERT INTO REFRESH_TOKEN (user_id, refresh_token, expired_at, issued_at, token_status, client_id, created_at, updated_at)
        VALUES (#{userId}, #{refreshToken}, #{expiredAt}, #{issuedAt}, #{tokenStatus}, #{clientId}, #{createdAt}, #{updatedAt})
    </insert>


</mapper>