<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sololiving.domain.follow.mapper.FollowMapper">

    <!-- 팔로우 추가 -->
    <insert id="insertFollow">
        INSERT INTO follow (follower, following, created_at)
        VALUES (#{follower}, #{following}, #{createdAt})
    </insert>

    <!-- 팔로우 삭제 -->
    <delete id="deleteFollow">
        DELETE FROM follow
        WHERE follower = #{follower} AND following = #{following}
    </delete>

    <!-- 팔로우 상태 확인 -->
    <select id="isAlreadyFollowing">
        SELECT follower, following
        FROM follow
        WHERE follower = #{follower} AND following = #{following}
    </select>

    <!-- 팔로잉 목록 조회 -->
    <select id="getFollowingList" resultType="string">
        SELECT following
        FROM follow
        WHERE follower = #{userId}
    </select>

    <!-- 팔로워 목록 조회 -->
    <select id="getFollowerList" resultType="string">
        SELECT follower
        FROM follow
        WHERE following = #{userId}
    </select>
    
</mapper>
